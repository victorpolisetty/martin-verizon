import React from 'react';
import ReactDOM from 'react-dom';
import ChatBot from './what_you_talking'; // Import the Website component

ReactDOM.render(
  <React.StrictMode>
    <ChatBot /> {/* Render the Website component */}
  </React.StrictMode>,
  document.getElementById('root')
);
